<!--pages/index/index.wxml-->
<view class='page'>
  <view class="page__hd">
        <!-- <image src='{{avatar}}'></image> -->
        <view style='font-size:40px' class="page__desc">这里到时候加一个快捷栏或者gallery把</view>
  </view>
    <view class="page__bd">
        <view class="weui-grids">
            <block wx:for="{{grids}}" wx:key="index">
                <navigator url="" class="weui-grid" style='border-style:none' hover-class="weui-grid_active">
                    <image class="weui-grid__icon"  src="{{item.src}}" />
                    <view class="weui-grid__label">{{item.name}}</view>
                </navigator>
            </block>
        </view>
    </view>
</view>
